server:
  port: 8081
  data:
    mongodb:
      uri: mongodb://localhost:27017/bankaccountdbgb
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    registryWithEureka: false
#    fetchRegistry: false
#    serviceUrl:
#     defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
