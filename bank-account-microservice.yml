server:
  port: 8080
  error:
    include-message: always
  data:
    mongodb:
      uri: mongodb+srv://nttdata:vZdLBB0KYwV2eU5C@bankaccuntdb.cypc0.mongodb.net/bankaccuntdb?retryWrites=true&w=majority
      #host: localhost
      #port: 27017
      #database: banktransactiondb
      #uri: mongodb://localhost:27017/bankaccountdb

microservices:
  client:
    base-url: http://localhost:8083
    base-path: /api/v1
    end-point:
      client: ${microservices.client.base-path}/clients
      plan: ${microservices.client.base-path}/plans
