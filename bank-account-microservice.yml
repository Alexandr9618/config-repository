server:
  port: 8080
  data:
    mongodb:
      uri: mongodb://localhost:27017/bankaccountdbg
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    registryWithEureka: false
#    fetchRegistry: false
#    serviceUrl:
#     defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
